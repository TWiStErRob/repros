plugins {
	id("org.jetbrains.kotlin.jvm") version "1.9.10"
}

// Dependencies so the repro can be imported into IDE and debugged with sources.
repositories {
	mavenCentral()
}
dependencies {
	implementation("org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.10")
}
