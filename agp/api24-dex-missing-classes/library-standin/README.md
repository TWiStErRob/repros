This module represents an external source for .class files,
it doesn't matter where they come from,
just that they're not directly compiled inside `:library-with-classes`.

In our case, it was a JAR that we repackaged and patched.
